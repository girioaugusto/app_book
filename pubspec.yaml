name: livros_app
description: App de livros
publish_to: "none"

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  provider: ^6.1.2          # gerenciar estado de forma simples
  shared_preferences: ^2.3.2 # persistir favoritos no dispositivo
  google_fonts: ^6.2.1
  url_launcher: ^6.3.0
  geolocator: ^13.0.1
  geocoding: ^3.0.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - lib/assets/logo.png

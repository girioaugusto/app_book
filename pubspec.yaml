name: livros_app
description: App de livros
publish_to: "none"

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # --- suas libs existentes ---
  http: ^1.2.2
  provider: ^6.1.2
  shared_preferences: ^2.3.2
  google_fonts: ^6.2.1
  url_launcher: ^6.3.0
  geolocator: ^14.0.2
  geocoding: ^4.0.0
  intl: ^0.20.2

  # --- login / storage ---
  sqflite: ^2.3.0
  path: ^1.8.3
  cryptography: ^2.5.0
  flutter_secure_storage: ^9.0.0

  # --- ADIÇÕES PARA SUPABASE ---
  supabase_flutter: ^2.6.0
  flutter_dotenv: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - lib/assets/logo.png
    - .env
